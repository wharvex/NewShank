define start()
	gcd 1220,516 
	gcd 128,32

define gcd (a,b : integer)
variables r : integer
    {Test comment} 
    r := 1/a
	while a mod b > 0 
		r := a mod b
		a := b
		b := r
	write b
    {Test comment}


define tester (a,b : integer)
    write "hello"
